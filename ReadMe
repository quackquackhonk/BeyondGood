SpreadsheetGUIViewEditable:
	- GUI for displaying editable spreadsheets
	- UI elements:
		- Formula bar that allows users to input text to create new cells
		- Highlighted cell: Highlights the currently selected cell. Defaults to A1 and
		                    can be changed using the arrow keys or with mouse click.
		- Inputs/Buttons to add columns and rows to the spreadsheet.
		- Row/Column headers: stickied so users know what cells are being displayed.


SpreadsheetGUIView:
	- Read-only GUI for displaying spreadsheets
	- UI elements:
		- Formula bar that is not editable.
		- Row/Column headers: stickied so users know what cells are being displayed.


